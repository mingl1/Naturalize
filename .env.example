# Antigravity Scrape-as-Code Engine Configuration

# LLM Providers API Keys (Configure at least one to enable LLM selector inference)
# If neither is set, the generator defaults to structural selector heuristics (mock mode)
GEMINI_API_KEY=your_gemini_api_key_here
OPENAI_API_KEY=your_openai_api_key_here

# Server Settings
HOST=127.0.0.1
PORT=8000
